# Frontend (Angular)

Requiere Node 18+.

```bash
cd frontend/angular
npm i
npm start   # abre en http://localhost:4200
```

Ajusta `API_BASE` en `src/app/core/api.service.ts` si cambias el host/puerto del backend.
